---

## **3️⃣ Backend README — `/server/README.md`**

```markdown
# SyncUp Backend

Backend API for the SyncUp platform — powered by **Node.js + Express** with a **MySQL database**.

## Current Phase

✅ Express server running (port 5000)  
✅ MySQL connection live (`syncup_local`)  
✅ `/api/users` & `/api/projects` routes configured  
✅ Modular folder structure established

### Stack

- **Node.js v20+**
- **Express 5**
- **MySQL2 (Promise Pool)**
- **dotenv 17+**
- **CORS**
- **Nodemon (dev)**

### Folder Structure
```
